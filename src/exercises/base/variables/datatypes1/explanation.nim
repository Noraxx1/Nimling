echo "Integers (that we covered before) can be used in mathematical calculations with basic operators."
echo "For example, `/` for division, `*` for multiplication, `+` for addition, and `-` for subtraction."
echo "We also have more advanced symbols such as `<` for less than and `>` for greater than."
