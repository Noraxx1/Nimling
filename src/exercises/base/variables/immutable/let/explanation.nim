


echo "In nim you declare a immutable variable whit `let`,the value of a let can be set at any time"
echo "but once is set it can't be changed anymore."

echo "For the rest is the same as declaring a normal `var`"